name: Build and Deploy to DigitalOcean

on:
  push:
    tags:
      - "v*"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to DigitalOcean Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DOCR_REGISTRY }}
          username: ${{ secrets.DOCR_USERNAME }}
          password: ${{ secrets.DOCR_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCR_REGISTRY }}/${{ secrets.APP_NAME }}:${{ github.ref_name }}
            ${{ secrets.DOCR_REGISTRY }}/${{ secrets.APP_NAME }}:latest

      # - name: Install doctl
      #   uses: digitalocean/action-doctl@v2
      #   with:
      #     token: ${{ secrets.DOCR_PASSWORD }}

      # - name: Update App Platform App
      #   run: |
      #     doctl apps update ${{ secrets.DO_APP_ID }} \
      #       --spec .do/app.yaml
